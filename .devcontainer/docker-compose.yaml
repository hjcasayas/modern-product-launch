services:
  dev-container:
    build:
      dockerfile: Dockerfile
      context: .
    volumes:
      - ../:/workspaces/browse-everything:cached
      - dev-container-node_modules:/workspaces/browse-everything/node_modules
    command: sleep infinity
volumes:
  dev-container-node_modules:
